apiVersion: v1
kind: Secret
metadata:
  name: fproject-backend-secret
  namespace: default
type: Opaque
data:
  # IMPORTANT: These are placeholder values
  # Actual secrets are injected by CI/CD pipeline from GitHub Secrets
  # For local development, use k8s/secret.local.yaml (not committed to Git)
  DB_PASSWORD: PLACEHOLDER_WILL_BE_REPLACED_BY_CICD
  GOOGLE_CLIENT_SECRET: PLACEHOLDER_WILL_BE_REPLACED_BY_CICD
